-- -- REQUIRES some nightly features, hence commented out
return {
  -- 'cordx56/rustowl',
  -- version = '*', -- Latest stable version
  -- build = 'cargo install rustowl',
  -- lazy = false, -- This plugin is already lazy
  -- opts = {},
}
