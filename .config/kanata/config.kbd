;; Configuring caps-lock to double as ctrl & esc

(defsrc
  caps
  rsft
)

(defalias
  escctrl (tap-hold 100 100 esc lctrl)
  rshft (layer-while-held rshift)
)

(deflayer base
  @escctrl
  @rshft
)
(deflayermap (rshift)
  lsft caps
)

