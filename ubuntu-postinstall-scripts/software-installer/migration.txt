- [x] Do a dry run of the install scripts on a VM
- [x] Check if dconf settings are still relevant
  - if some features are made default
  - if some options are depricated
- [x] Make note of software installed manually
- [x] GSConnect & KDEConnect settings into Notion
- [x] Sync Anki
- [x] Hermes backup memories/, skills/, terminal-sessions/, sessions/, config.yaml, .env
- [ ] Backup ALL Brave profiles in another device & check if stuff like bookmarks still work
- [ ] Backup ~/{Desktop,Documents,Downloads,Pictures,Projects,Transfers,Videos}
  - backup desktop & projects
  - [x] Backup $ZDOTDIR/personal/
  - [x] Backup Game save data like Rocket League replay saves
- REMOVE ME: remove all lines with warn in dconf.nix

- autoinstall.yaml
  - Change timezone if necessary
  - Add user password hash via:     openssl passwd -6
  - Check serving machine's IP via: hostname -I
  - Initiate LAN transfer via:      python3 -m http.server <port>
      - Ensure that your firewall is not blocking <port>
  - Access from the yaml file as - http://<host-ip>:<port>/autoinstall.yaml

